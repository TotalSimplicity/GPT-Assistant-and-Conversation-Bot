# GPT-Assistant-and-Conversation-Bot

INSTALLATION/USAGE:

pip install -r requirements.txt

If on linux:

sudo apt install portaudio19-dev

pip install pyaudio

To run:

python main.py [nogui/gui] [assistant/conversation/debate]
